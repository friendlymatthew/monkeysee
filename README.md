## A Chrome Extension using Leptos

### Usage
After making a change to the app, make sure to run:

```shell
npm run build
```

Which will automatically update your most recently compiled `.js` file into `popup.html`